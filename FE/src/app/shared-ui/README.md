# shared-ui (placeholder)

Reserved for the common design/component library the team hasn't built yet.
No code here on purpose — this scaffold is a draft (`FE/`), not the final
frontend, and the real component library will replace this folder's contents
when it exists.
